{
  "name": "azov-resort-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "start": "npm --prefix app start",
    "lint": "npm --prefix app run lint",
    "postinstall": "npm --prefix app install"
  }
}
